name: GitHub Actions Demo Create File
run-name: ${{ github.actor }} is testing out GitHub Actions, this time create a file
on: push
permissions:
  contents: write
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "checkout Repo"
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: echo "Build SW"
      - run: kotlinc -Werror hello.kt -include-runtime -d hello.jar
     # - name: List files in the repository
      - run: echo "hello world" >> new_file.txt
      - run: ls ${{ github.workspace }}
      - name: Commit report
        run: |
          git config --global user.name 'RJPlog'
          git config --global user.email 'tfrenz67@gmail.com'
          git add new_file.txt
          git commit -m "Automated report"
          git push
